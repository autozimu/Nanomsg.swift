import CNanomsg

/** Current interface version. */
public let NN_VERSION_CURRENT = CNanomsg.NN_VERSION_CURRENT

/** Revision of current interface. */
public let NN_VERSION_REVISION = CNanomsg.NN_VERSION_REVISION

/** How many past interface versions are still supported. */
public let NN_VERSION_AGE = CNanomsg.NN_VERSION_AGE

/** Zero-copy message size. */
let NN_MSG = 4294967295
// NN_MSG = ((size_t) - 1)
